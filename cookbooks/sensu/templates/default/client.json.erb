{
  "client": {
    "name": "<%= node[:fqdn] %>",
    "address": "<%= node[:ipaddress] %>",
    "subscriptions": [ "test"
    <% @subscriptions.each do |sub| -%>
      ,"<%= sub %>"
    <% end -%>
    ]
  }
}
