{
  "checks": {
    "cron_check": {
      "handlers": ["librato", "irc"],
      "command": "/usr/local/etc/sensu/plugins/check-procs.rb -p cron -C 1 -c 10 -w 5",
      "interval": 60,
      "refresh": 300,
      "subscribers": [ "servers" ]
    }
  }
}
