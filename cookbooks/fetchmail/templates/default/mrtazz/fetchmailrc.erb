<% @accounts.each do |a| %>
server <%= a["server"] %>
proto  <%= a["proto"] %>
user   <%= a["user"] %> smtpname <%= a["smtpname"] %>
pass   "<%= a["password"] %>"
ssl
sslcertfile /usr/local/share/certs/ca-root-nss.crt

<% end %>
